public interface Predator { // the keyword "interface" makes this an interface 
     
    // INTERFACES (VIDEO 41)

    void hunt(); // we are declaring a method, but not implementing it, because it is the job of the classes to implement this 

    void kill(); // we are declaring another method of all predators, but not implementing it 

    


}
