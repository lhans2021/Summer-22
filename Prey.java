public interface Prey { // the keyword "interface" makes this an interface

    // INTERFACES (VIDEO 41)

    void run(); // we are declaring a method--run-- that all prey have, but it's the class' job to implement it 

    void hide();  // we are declaring another method that classes will implement 


}
